[workspace]
members = [ "component_registry",
    "demo_platformer",
    "editor",
    "engine",
    "engine_protocol",
    "engine_types",
    "prefab_compiler",
]
resolver = "3"

[workspace.dependencies]
hecs = { version = "0.10" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
